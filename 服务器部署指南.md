# 服务器部署和运行指南

## 服务器配置
- **GPU**: V100 32G
- **项目仓库**: https://gitee.com/qiu-jianbo555/reproduce.git

---

## 📋 快速部署步骤

### 1. 连接到服务器
```bash
ssh your_username@your_server_ip
# 例如: ssh root@xxx.xxx.xxx.xxx
```

### 2. 上传部署脚本
在**本地**执行（将脚本上传到服务器）：
```bash
scp deploy_to_server.sh your_username@your_server_ip:~/
scp run_server.sh your_username@your_server_ip:~/
```

或者在服务器上直接创建这些脚本文件。

### 3. 在服务器上执行部署
```bash
# 给脚本添加执行权限
chmod +x deploy_to_server.sh run_server.sh

# 执行部署
bash deploy_to_server.sh
```

### 4. 运行项目
```bash
cd reproduce
bash ../run_server.sh
```

---

## 🚀 运行选项

### 方式一：后台运行（推荐）
```bash
bash run_server.sh
```
这会在后台运行项目，日志保存到 `server_run.log`

### 方式二：直接运行（前台）
```bash
python run_test.py
```

### 方式三：使用screen（推荐用于长时间运行）
```bash
# 创建新的screen会话
screen -S watermark_test

# 运行项目
python run_test.py

# 断开会话（项目继续运行）: Ctrl+A 然后按 D
# 重新连接: screen -r watermark_test
# 查看所有会话: screen -ls
```

### 方式四：使用tmux
```bash
# 创建新的tmux会话
tmux new -s watermark_test

# 运行项目
python run_test.py

# 断开会话: Ctrl+B 然后按 D
# 重新连接: tmux attach -t watermark_test
```

---

## 📊 监控和管理

### 查看实时日志
```bash
tail -f server_run.log
```

### 查看GPU使用情况
```bash
# 实时监控
watch -n 1 nvidia-smi

# 或者
nvidia-smi -l 1
```

### 查看进程状态
```bash
# 查看PID
cat server_run.pid

# 查看进程
ps aux | grep python
```

### 停止运行
```bash
# 方式1: 使用保存的PID
kill $(cat server_run.pid)

# 方式2: 手动查找并停止
ps aux | grep run_test.py
kill <PID>
```

---

## 🔧 常见问题

### 1. 依赖安装失败
```bash
# 使用清华镜像源
pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# 或者手动安装主要依赖
pip install torch torchvision transformers datasets -i https://pypi.tuna.tsinghua.edu.cn/simple
```

### 2. 显存不足
项目已针对V100 32G优化，使用了：
- FP16混合精度
- 显存清理
- PPL优化

如果仍然遇到显存问题，可以调整批次大小或样本数量。

### 3. SSH连接断开后进程停止
使用 `screen` 或 `tmux` 来保持进程运行（见上方"运行选项"）。

### 4. 查看项目运行进度
```bash
# 查看完整日志
less server_run.log

# 搜索特定内容
grep "error" server_run.log
grep "完成" server_run.log
```

---

## 📦 环境要求

- Python 3.8+
- PyTorch (with CUDA support)
- transformers
- datasets
- numpy, pandas
- 其他依赖见 requirements.txt

---

## 📝 运行特定测试

如果不想运行完整的 `run_test.py`，可以运行单个测试：

```bash
# 例如运行特定测试
python test_official_watermarks_quick.py
python test_icw_quick.py
python test_xsir_quick.py
# ... 等等
```

---

## 💾 同步结果回本地

测试完成后，将结果同步回本地：

```bash
# 在本地执行
scp your_username@your_server_ip:~/reproduce/server_run.log ./
scp your_username@your_server_ip:~/reproduce/output_final.txt ./
```

---

## 🔄 更新代码

如果本地代码有更新，需要同步到服务器：

```bash
# 在本地推送到Gitee
git add .
git commit -m "更新说明"
git push gitee master

# 在服务器上拉取更新
cd reproduce
git pull origin master
```
